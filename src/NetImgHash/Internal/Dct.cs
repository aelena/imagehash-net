namespace NetImgHash.Internal;

internal static class Dct
{
    public static double[] Transform2D(ReadOnlySpan<byte> samples, int dimension)
    {
        throw new NotSupportedException("The pHash DCT implementation is planned for a follow-up release.");
    }
}
